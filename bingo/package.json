{
  "name": "bingo",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://naka1998.github.io/for_public_naka1998/",
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "build": "react-scripts build",
    "eject": "react-scripts eject",
    "postinstall": "gulp browserify",
    "start": "node server/server.js",
    "deploy": "npm run build && gh-pages -d build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "gulp": "^4.0.2"
  }
}
